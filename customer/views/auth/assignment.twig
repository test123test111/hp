{% set menu_name = "managers" %}
{% set sidebar_name = "分配权限" %}
{% extends "@app/views/layouts/main.twig" %}
{% block content %}
<div class="page-content">
      <div class="page-header">
        <h1>
          Setting Assignment
        </h1>
      </div>
      <div class="row">
        <div class="col-xs-12">
          <!-- PAGE CONTENT BEGINS -->

          {% set form = activeform.begin({'id':'create-form','options':{'enctype':'multipart/form-data'}}) %}
                <div class="form-group field-authitem-name required">
                    <label for="authitem-name" class="checkbox inline">Assign Roles to Users*</label>
                    <div class="checkbox inline" id="authitem-name">
                        {% for v in allRoles %}
                        <div class="checkbox">
                            <label>
                            <input type="checkbox" value="{{ v }}" name="child[role][]"{% for item in roles %}{% if item == v %} checked{% endif %}{% endfor %}> {{ v }}</label>
                        </div>
                        {% endfor %}
                        <div class="help-block"></div>
                    </div>
                    <div class="form-group">
                        <input type='submit' value='提交' class='btn btn-large btn-primary' /> 
                    </div>
                </div>
                {{ void(form.end()) }}
        </div><!-- /.col -->
      </div><!-- /.row -->
</div>
{% endblock %}
